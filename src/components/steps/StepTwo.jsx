// This file is no longer used in the primary quiz flow.
// It is kept in the project to avoid breaking imports if it's referenced elsewhere,
// but it is effectively deprecated.
import React from 'react';

const DeprecatedStepTwo = () => {
    return null;
}

export default DeprecatedStepTwo;